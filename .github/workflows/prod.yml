name: "Prod Build"
run-name: "3.0.${{ github.run_number }}"

env:
  FW_VERSION: "3.0.${{ github.run_number }}"
  FW_VERSION_DEVICE: "3.0.${{ github.run_number }}"

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Restore PlatformIO Cache
        id: cache-pio-restore
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.platformio
            .pio
          key: ${{ runner.os }}-pio-${{ hashFiles('deploy/platformio.ini') }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-pio-${{ hashFiles('deploy/platformio.ini') }}-
            ${{ runner.os }}-pio-

      - name: Install PlatformIO
        run: |
          python -m pip install --upgrade pip
          pip install platformio

      - name: Inject secrets.h
        env:
          SECRETS_H: ${{ secrets.SECRETS_H }}
        run: |
          echo "$SECRETS_H" > src/secrets.h

      - name: Inject .env
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
        run: |
          echo "$ENV_FILE" > .env

      - name: Set version and debug flag
        run: |
          newversion="${{ env.FW_VERSION_DEVICE }}"
          
          # Replace version in types.h
          sed -i -e "s/0\.0\.0/$newversion/g" src/types.h
          
          # Replace DEBUG flag in types.h
          sed -i -E "s/#define DEBUG [01]/#define DEBUG 0/g" src/types.h
          
          # Remove test() call from main.cpp
          sed -i '/test();.*please remove/d' src/main.cpp
          
          # Replace FOTA JSON content
          sed -i -e "s/X.X.X/$newversion/g" deploy/espfota.json
          sed -i -e "s|http://sample.de/firmware.bin|http://${{ secrets.S3_BUCKET_NAME }}/firmware.bin|g" deploy/espfota.json
          sed -i -e "s/firmware.bin/firmware_epd13.bin/g" deploy/espfota.json
          cp ./deploy/platformio.ini ./platformio.ini

      - name: Install PlatformIO Dependencies
        run: pio pkg install

      - name: Run PlatformIO Build
        id: build
        run: |
          if ! pio run; then
            echo "::warning::Build with cache failed. Retrying clean build..."
            rm -rf .pio
            pio pkg install
            pio run
            echo "rebuild=true" >> $GITHUB_OUTPUT
          fi

      - name: Save PlatformIO Cache
        if: steps.cache-pio-restore.outputs.cache-matched-key == '' || steps.build.outputs.rebuild == 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.platformio
            .pio
          key: ${{ runner.os }}-pio-${{ hashFiles('deploy/platformio.ini') }}-${{ github.run_id }}

      - name: Verify and Prepare Artifacts
        run: |
          if [ ! -f "./.pio/build/ESP32-C6-DevKitM-1/firmware.bin" ]; then
            echo "::error::Build failed: firmware.bin was not created."
            exit 1
          fi
          
          ls -la
          mkdir artifact
          cp ./.pio/build/ESP32-C6-DevKitM-1/firmware.bin ./artifact/firmware_epd13.bin
          cp ./deploy/espfota.json ./artifact/espfota_epd13.json

      - name: Publish Pipeline Artifact
        uses: actions/upload-artifact@v4
        with:
          name: epaper_fw_epd13_${{ env.FW_VERSION }}
          path: artifact/

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: S3 Upload Firmware and JSON
        run: |
          # Upload files to the root of the bucket with public-read ACL
          aws s3 cp ./artifact/ s3://${{ secrets.S3_BUCKET_NAME }}/ --recursive --exclude "*" --include "firmware_epd13*" --include "espfota_epd13*" --acl public-read

      - name: Create Archive
        run: |
          # Create a ZIP archive of the artifacts
          cd artifact
          zip -r ../epaper_fw_epd13_${{ env.FW_VERSION }}.zip ./*
          cd ..

      - name: S3 Upload Archive
        run: |
          # Upload the ZIP archive to the root of the bucket
          aws s3 cp epaper_fw_epd13_${{ env.FW_VERSION }}.zip s3://${{ secrets.S3_BUCKET_NAME }}/ --acl public-read

